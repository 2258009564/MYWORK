#include <bits/stdc++.h>
using namespace std;

int main(){
    int n,c;
    cin >> n >> c;
    vector<int> vec(n);
    for(auto &num:vec){
        cin >> num;
    }
    //sort first
    sort(vec.begin(), vec.end());
    long long ans = 0;
    //three pointers is too hard for me!
    for (int i = 1, j = 1, k = 1; i < n;i++){

    }
}