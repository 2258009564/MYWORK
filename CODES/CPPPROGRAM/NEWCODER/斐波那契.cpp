// #pragma GCC optimize(2)
#include <bits/stdc++.h>
using namespace std;
#define int long long
#define endl '\n'

int f(int i, int x, int y)
{
    i %= 3;
    if (i == 1)
    {
        return x;
    }
    else if (i == 2)
    {
        return y;
    }
    else
    {
        return x + y;
    }
}

void solve()
{
"    $0",
}

signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int t = 1;
    cin >> t;
    while(t--)
    {
        solve();
    }
}