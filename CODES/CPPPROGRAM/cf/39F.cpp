#include <bits/stdc++.h>
using namespace std;
#define int long long
#define endl '\n'

signed main()
{
    int n, m, k;
    cin >> n >> m >> k;
    vector<int> frogs(m), sks(k);
    for (auto &&i : frogs)
    {
        cin >> i;
    }
    for (auto &&i : sks)
    {
        cin >> i;
    }
    
}