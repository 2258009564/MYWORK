// #pragma GCC optimize(2)
#include <bits/stdc++.h>
using namespace std;
#define int long long
#define endl '\n'

bool cmp(string str1 , string str2,)
{
    int maxindex = min(str1.size(), str2.size());
    

}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int n;
    cin >> n;
    string str = "";
    string str1;
    vector<string> vec;
    while(cin>>str1)
    {
        vec.push_back(str1);
    }
    
    sort(vec.begin(), vec.end(), cmp);
}