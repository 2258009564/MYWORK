## 命令的基础格式
```linux
command [-options] [parameter] 
```
语法中的 `[]` 表示可选的意思，也就是非必填
- command 命令本身
- -options 通过该选项控制命令的执行细节
- parameter 多数用于命令的指向目标 


## ls
```
ls [-a -l -h] [linux路径]
```

1. ls命令的参数的作用
可以指定要查看的文件夹（目录）的内容，如果不给定参数，就查看当前工作目录的内容
2. ls命令的选项：
	- -a 可以展示出隐藏的内容，以 `.` 开头的文件或文件夹默认被隐藏，需要 `-a` 才能显示出来
	- -l 以列表的形式展示内容，并展示更多细节
	- -h 需要和 `-l` 选项搭配使用，以更加人性化的方式显示文件的大小单位
3. 命令的选项组合使用
	命令的选项是可以组合使用的，比如：`ls -alh`，等同于 `ls -a -l -h`

## cd
```
cd [linux路径]
```

cd来自英文 Change Directory
用途是 切换路径
直接使用 切换到home

## pwd
```
pwd
```

pwd来自英文 Print Work Directory
用途是 输出当前所在的工作目录

## 相对路径 绝对路径 特殊路径符号
1. 相对路径和绝对路径
	- 绝对路径：以根目录做起点，描述路径的方式，路径以/开头
	- 相对路径：以当前目录故起点，描述路径的方式，路径不需以/开头
	
2. 特殊路径符有哪些？
	- . 表示当前目录，比如cd .，或cd ./Desktop
	- .. 表示上一级目录，比如：cd.. 或cd ../
	- ~ 表示用户的HOME目录，比如：cd～或 cd ~/Desktop

## mkdir
```
mkdir [-p] Linux路径
```

mkdir用来创造新的目录（文件夹）
参数必填 表示需要创建的目录的路径

- -p 可选 表示自动创建不存在的父目录 适用于创建连续多层级的目录

## 文件操作
### touch
```
touch Linux路径
```

通过touch 命令创建文件

### cat
```
cat Linux路径
```

通过 cat 命令 直接查看文件内容

### more
```
more Linux路径
```

more路径同样用于查看文件内容 与cat不同的是：
- cat直接将内容全部显示出来
- more支持翻页 如果文件内容过多 可以一页页的展示

more 使用 space 翻页，使用 q 退出查看

### cp
```
cp [-r] 参数1 参数2
```

cp 来源于英文 Copy
- -r 用于复制文件夹使用 表示递归
- 参数1 Linux路径 表示被复制的文件/文件夹
- 参数2 Linux路径 表示要复制去的地方

### mv
```
mv 参数1 参数2
```

mv 来源于英文 move

- 参数1 Linux路径 表示被移动的文件/文件夹
- 参数2 Linux路径 表示要移动去的地方  如果目标不存在 则进行改名 确保目标存在

### rm
```
rm [-r -f] 参数1 参数2 ... 参数n
```

- -r 可选 文件夹删除
- -f 可选 强制删除 (force) 
- 参数 表示被删除的文件/文件夹路径 支持多个 空格隔开
- 参数支持通配符 * 用来模糊匹配

| **命令**                   | **结果**        | **适用场景**                      |
| ------------------------ | ------------- | ----------------------------- |
| **`rm -rf folder`**      | 文件夹消失，里面东西也没了 | 这个项目/任务彻底结束了，不需要这个目录了。        |
| **`rm -rf folder/*`**    | 文件夹还在，里面清空了   | 比如清理缓存（Cache）或日志（Log），目录必须留着。 |
| **`rm -f folder/*.tmp`** | 文件夹在，只删特定类型文件 | 精细化管理，只清理垃圾，保留重要数据。           |

## 查找



















