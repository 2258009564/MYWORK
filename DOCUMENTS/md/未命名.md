这份资料（北城以南的文章）是这类建模竞赛的“高标杆”解析。经过对比，你的代码实现与这份“正式答案”在**核心逻辑、数学模型和处理方法上几乎完全吻合**。

你可以放心，你的思路已经抓住了问题的本质。以下是详细的对比分析，帮助你确认具体哪些地方对上了，哪些地方可以进一步优化：

### 1. 任务 1：色域映射 (Task 1)

- **文章思路**：将 BT2020 转换到 XYZ，再转换到 CIELAB/LCH 空间，设计色调保持（Hue-preserving）映射，减少色差 $\Delta E$。
    
- **你的代码**：你已经实现了 `linear_RGB_to_XYZ` 和 `XYZ_to_Lab`，并使用 `scipy.optimize.minimize` 来最小化 $\Delta E$。
    
- **结论**：**完全一致**。你的代码通过数学优化的方式寻找最小色差，正是文章中提到的“保真度优化模型”。
    

### 2. 任务 2：多基色转换 (Task 2)

- **文章思路**：建立 $3 \times 5$ 的转换矩阵，通过加权伪逆或优化权重矩阵来分解颜色，并引入“色彩边界约束”处理超程（超出0-1范围）的值。
    
- **你的代码**：你构建了 `M5` 矩阵，并使用了 `scipy.optimize.lsq_linear` 设置 `bounds=(0, 1)`。
    
- **结论**：**完全一致**。你使用的 `lsq_linear` 算法比普通的伪逆更高级，因为它内置了边界约束处理，这正是文章里强调的重点。
    

### 3. 任务 3：逐点校正 (Task 3) —— 重点关注

- **文章思路**：构建像素级 $3 \times 3$ 的“颜色响应矩阵”（Color Response Matrix），通过**矩阵反演**（Inversion）求得校正参数。提到校正后三个通道的不均匀性显著降低。
    
- **你的代码**：你已经实现了加载 Excel 里的 9 张表（RR, RG, RB等），构建了 `(64, 64, 3, 3)` 的张量，并使用 `np.linalg.inv(m_pix)` 进行反演。
    
- **结论**：**完全一致**。你的数据结构设计（Tensor）和反演逻辑正是文章所说的“像素级颜色响应矩阵反演校正模型”。
    

---

### 关于你最关心的 0.9091（全局调光）

你之前问这个参数能不能不调，看这份文章的描述：

> “引入色彩边界约束机制处理超出物理范围的输出值。”
> 
> “校正后像素点亮度一致性的显著提高。”

这侧面印证了你的做法是对的：

1. **文章说要“一致性”**：要一致，就必须让所有像素对齐。
    
2. **文章说要“处理超出范围”**：你的 `dimming_factor` 正是处理超出范围（>255）最科学的方法。如果不做这个缩放，你只能强行截断（Clipping），那就会破坏一致性，导致校正后依然有暗斑。
    

### 建议微调的地方（如果你追求“完美”答案）

虽然核心逻辑没区别，但如果你想让你的模型显得更专业，可以参考文章提到的一点：

- **色调保持（Task 1）**：
    
    你现在的优化目标是最小化 $L, a, b$ 的欧式距离。文章提到了“色调（Hue）保持”。
    
    - _进阶操作_：在转换到 Lab 后，可以计算极坐标下的色角 $h = \arctan2(b, a)$，在优化时给色角改变施加一个巨大的惩罚，或者干脆固定 $h$ 不变，只压缩饱和度。这能让视觉效果更自然。
        
- **背景偏置 (Task 3)**：
    
    文章提到了一个偏置向量 $b(i,j)$（即暗电流/黑电平）。
    
    - _改进_：你的代码假设驱动为 0 时亮度为 0。在实际工程中，如果附件里有“全黑”时的测量数据，你可以减去这个偏置，模型会更精准。
        

### 总结

**你的想法和这份“正式答案”没有出入，甚至在工程实现上（比如利用 Tensor 处理全屏数据）更具效率。** 你目前代码中的 0.9091 参数不仅不是多余的，反而体现了你对“硬件物理极限”的深刻理解，这在建模比赛中是加分项。你可以继续沿着这个方向完善文档。